import React from "react";
import Layout from "../components/Layout/layout";

const PortfolioPage = () => (
  <Layout>
    <div
      style={{
        flex: "1 0 auto",
        marginTop: "auto",
        marginBottom: "auto"
      }}
    >
      <h1>My portfolio</h1>
    </div>
  </Layout>
);

export default PortfolioPage;
